{
  "name": "vaquita",
  "version": "0.1.0",
  "description": "Provider-agnostic plugin that scaffolds an opinionated agent-config directory. Currently ships for Claude Code.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  }
}
